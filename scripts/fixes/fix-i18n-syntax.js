#!/usr/bin/env nodeconst fs = require('fs');',const path = require('path');',console.log(', Fixing i18n syntax errors...');',const filePath = path.join(__dirname, 'src', 'i18n', 'index.ts');',try {  let content = fs.readFileSync(filePath, 'utf8');'    // Fix object keys with extra comma before colon  // Pattern: "key, : "value" -> "key": "value",  content = content.replace(/"]+)",\s*:\s*/g, '"$1": ');'    // Also fix similar pattern with single quotes  content = content.replace(/']+)',\s*:\s*/g, "'$1': ");",    fs.writeFileSync(filePath, content, 'utf8');',  console.log(', Fixed i18n/index.ts');'  }, catch (error) {  console.error(', Error fixing i18n/index.ts:', error.message);',  process.exit(1);},console.log(', All fixes applied successfully!');'