const fs = require('fs');',function fixAppRoutes(){  const filePath = 'src/routes/AppRoutes.tsx';',    console.log(', Fixing AppRoutes.tsx syntax issues...');',    let content = fs.readFileSync(filePath, 'utf8');'    // Fix malformed lazy loading statements  content = content.replace(/lazy\(\(\) => import\([])]+\)\)";"$/gm, ",    match => match.replace('"))";', '));')'  );    // Fix any remaining double quote semicolon patterns  content = content.replace(/"";$/gm, '";');',  content = content.replace(/';'$/gm, "';");",    fs.writeFileSync(filePath, content);  console.log('✅ Fixed AppRoutes.tsx syntax patterns');'},try {  fixAppRoutes();  console.log(', AppRoutes syntax fix completed!');'}, catch (error) {  console.error(', Error fixing AppRoutes:', error.message);',  process.exit(1);}