const fs = require('fs');',const content = fs.readFileSync('C: /Users/damat/_REPOS/CoreV2/src/services/performanceMonitoringService.ts, 'utf8');',const lines = content.split('n');'// Find lines with odd number of quotesfor (let i = 0; i < lines.length; i++) {  const line = lines[]];  const doubleCount = (line.match(/"/g) || []).length;",  const singleCount = (line.match(/'/g) || []).length;',    if (doubleCount % 2 !== 0) {    console.log('Line', i + 1, 'has odd double quotes:', doubleCount);',    console.log(',  Content:', line.substring(0, 100);'  },    if (singleCount % 2 !== 0) {    console.log('Line', i + 1, 'has odd single quotes:', singleCount);',    if (i > 660) { // Focus on end of file      console.log(',  Content:', line.substring(0, 100);'    }  }}